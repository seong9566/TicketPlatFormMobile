class AppConstants {
  const AppConstants._();

  static const defaultPageSize = 16;
  static const networkTimeout = Duration(seconds: 30);
  static const fontFamily = 'SUIT';
  static const String paymentSuccessMessage = '결제가 완료되었습니다. 상품을 전송해주세요.';
}
